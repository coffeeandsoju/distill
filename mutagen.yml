sync:
  defaults:
    flushOnCreate: true
    ignore:
      vcs: true
      paths:
        - "mutagen.yml"
        - "mutagen.yml.lock"
        - "lightning_logs"

  solving:
    alpha: .
    beta: matt@solving:~/fundamentals/distill

  supercloud:
    alpha: .
    beta: mattfeng@txe1-login.mit.edu:~/fundamentals/distill
